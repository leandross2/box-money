import styled from 'styled-components';

export const Container = styled.button`
  background-color: var(--green);
  padding: 1rem;
  border: 0;
  border-radius: .5rem;
  color: var(--white);
  font-size: 1rem;
`;
